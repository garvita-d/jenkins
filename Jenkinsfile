pipeline {
    agent any  // Uses any available agent

    stages {
        stage('Build') {
            steps {
                echo "Building on ${env.NODE_NAME}"
                // Your build steps here
            }
        }
    }
}
